@extends('layouts.admin') @section('page_title','Support Tickets') @section('content')<div class="panel p-3 table-responsive"><table class="table"><thead><tr><th>Ticket</th><th>Subject</th><th>Customer</th><th>Priority</th><th>Status</th><th></th></tr></thead><tbody>@foreach($tickets as $ticket)<tr><td>{{ $ticket->ticket_number }}</td><td>{{ $ticket->subject }}</td><td>{{ $ticket->user?->email }}</td><td><span class="badge bg-warning">{{ $ticket->priority }}</span></td><td><span class="badge bg-primary">{{ $ticket->status }}</span></td><td><a href="{{ route('admin.support-tickets.show',$ticket) }}" class="btn btn-sm btn-outline-primary">Open</a></td></tr>@endforeach</tbody></table>{{ $tickets->links() }}</div>@endsection