@extends('layouts.admin') @section('page_title','Orders') @section('content')<div class="d-flex justify-content-between mb-3"><form class="d-flex gap-2"><input name="search" class="form-control" placeholder="Search order/customer"><button class="btn btn-dark">Search</button></form><a href="{{ route('admin.orders.create') }}" class="btn btn-primary">Create Order</a></div><div class="panel p-3 table-responsive"><table class="table"><thead><tr><th>Order</th><th>Customer</th><th>Status</th><th>Payment</th><th>Total</th><th></th></tr></thead><tbody>@foreach($orders as $order)<tr><td>{{ $order->order_number }}</td><td>{{ $order->customer_name }}<div class="small text-muted">{{ $order->customer_email }}</div></td><td><span class="badge bg-primary">{{ $order->status }}</span></td><td><span class="badge bg-secondary">{{ $order->payment_status }}</span></td><td>${{ number_format($order->total,2) }}</td><td><a href="{{ route('admin.orders.show',$order) }}" class="btn btn-sm btn-outline-primary">View</a></td></tr>@endforeach</tbody></table>{{ $orders->links() }}</div>@endsection