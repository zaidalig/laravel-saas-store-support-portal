@extends('layouts.admin') @section('page_title','Invoices') @section('content')<div class="panel p-4 mb-4"><form method="POST" action="{{ route('admin.invoices.store') }}" class="row g-2">@csrf <div class="col-md-10"><select name="order_id" class="form-select">@foreach($orders as $order)<option value="{{ $order->id }}">{{ $order->order_number }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Generate</button></div></form></div><div class="panel p-3 table-responsive"><table class="table"><thead><tr><th>Invoice</th><th>Order</th><th>Status</th><th>Total</th><th></th></tr></thead><tbody>@foreach($invoices as $invoice)<tr><td>{{ $invoice->invoice_number }}</td><td>{{ $invoice->order?->order_number }}</td><td><span class="badge bg-primary">{{ $invoice->status }}</span></td><td>${{ number_format($invoice->total,2) }}</td><td><a href="{{ route('admin.invoices.show',$invoice) }}" class="btn btn-sm btn-outline-primary">View</a></td></tr>@endforeach</tbody></table>{{ $invoices->links() }}</div>@endsection