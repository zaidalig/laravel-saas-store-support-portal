@extends('layouts.admin') @section('page_title','Printable Invoice') @section('content')<div class="panel p-5"><div class="d-flex justify-content-between"><h1>Invoice</h1><button onclick="window.print()" class="btn btn-primary no-print">Print</button></div><h4>{{ $invoice->invoice_number }}</h4><p>Issue: {{ $invoice->issue_date->format('M d, Y') }} | Due: {{ $invoice->due_date?->format('M d, Y') }}</p><table class="table mt-4">@foreach($invoice->order->items as $item)<tr><td>{{ $item->product_name }}</td><td>{{ $item->quantity }}</td><td>${{ number_format($item->total,2) }}</td></tr>@endforeach</table><h3 class="text-end">Total: ${{ number_format($invoice->total,2) }}</h3></div>@endsection