@extends('layouts.admin') @section('page_title','Admin Dashboard') @section('content')<div class="row g-3 mb-4">@foreach(['Users'=>$totalUsers,'Customers'=>$totalCustomers,'Products'=>$totalProducts,'Orders'=>$totalOrders,'Pending Orders'=>$pendingOrders,'Paid Invoices'=>$paidInvoices,'Open Tickets'=>$openTickets,'Contact Messages'=>$contactMessages] as $label=>$count)<div class="col-md-3"><div class="stat-card p-3"><p class="text-muted mb-1">{{ $label }}</p><h3 class="fw-bold mb-0">{{ $count }}</h3></div></div>@endforeach</div><div class="panel p-3"><h5 class="fw-bold">Recent Activity</h5><table class="table"><tbody>@foreach($logs as $log)<tr><td>{{ $log->created_at?->format('M d, H:i') }}</td><td>{{ $log->module }}</td><td>{{ $log->description }}</td></tr>@endforeach</tbody></table></div>@endsection