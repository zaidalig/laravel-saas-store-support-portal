@extends('layouts.admin') @section('page_title','Payments') @section('content')<div class="panel p-4 mb-4"><form method="POST" action="{{ route('admin.payments.store') }}" class="row g-2">@csrf <div class="col-md-3"><select name="order_id" class="form-select">@foreach($orders as $order)<option value="{{ $order->id }}">{{ $order->order_number }}</option>@endforeach</select></div><div class="col-md-2"><input name="amount" type="number" step="0.01" class="form-control" placeholder="Amount" required></div><div class="col-md-2"><select name="payment_method" class="form-select"><option>cash</option><option>bank_transfer</option><option>card</option><option>other</option></select></div><div class="col-md-2"><select name="status" class="form-select"><option>pending</option><option>completed</option><option>failed</option><option>refunded</option></select></div><div class="col-md-2"><input name="paid_at" type="datetime-local" class="form-control"></div><div class="col-md-1"><button class="btn btn-primary w-100">Add</button></div></form></div><div class="panel p-3 table-responsive"><table class="table"><thead><tr><th>Payment</th><th>Order</th><th>Method</th><th>Status</th><th>Amount</th><th>Update</th></tr></thead><tbody>@foreach($payments as $payment)<tr><td>{{ $payment->payment_number }}</td><td>{{ $payment->order?->order_number }}</td><td>{{ $payment->payment_method }}</td><td><span class="badge bg-primary">{{ $payment->status }}</span></td><td>${{ number_format($payment->amount,2) }}</td><td><form method="POST" action="{{ route('admin.payments.update',$payment) }}" class="d-flex gap-1">@csrf @method('PUT')<select name="status" class="form-select form-select-sm">@foreach(['pending','completed','failed','refunded'] as $s)<option {{ $payment->status===$s?'selected':'' }}>{{ $s }}</option>@endforeach</select><button class="btn btn-sm btn-outline-primary">Save</button></form></td></tr>@endforeach</tbody></table>{{ $payments->links() }}</div>@endsection
