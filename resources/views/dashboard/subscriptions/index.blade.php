@extends('layouts.dashboard') @section('page_title','My Subscriptions') @section('content')<div class="panel p-3 table-responsive"><table class="table align-middle"><thead><tr><th>Subscription</th><th>Plan</th><th>Status</th><th>Starts</th><th>Ends</th><th></th></tr></thead><tbody>@forelse($subscriptions as $subscription)<tr><td>{{ $subscription->subscription_number }}</td><td>{{ $subscription->pricingPlan->name }}</td><td><span class="badge bg-primary">{{ $subscription->status }}</span></td><td>{{ $subscription->starts_at->format('M d, Y') }}</td><td>{{ $subscription->ends_at->format('M d, Y') }}</td><td>@if($subscription->status === 'active')<form method="POST" action="{{ route('dashboard.subscriptions.cancel', $subscription) }}" class="d-inline">@csrf<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Cancel this subscription?')">Cancel</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-4">No subscriptions yet. <a href="{{ route('pricing') }}">Browse plans</a>.</td></tr>@endforelse</tbody></table>{{ $subscriptions->links() }}</div>@endsection
