@extends('layouts.dashboard') @section('page_title',$ticket->ticket_number) @section('content')<div class="panel p-4 mb-4"><h4 class="fw-bold">{{ $ticket->subject }}</h4><p>{{ $ticket->message }}</p><span class="badge bg-warning">{{ $ticket->priority }}</span> <span class="badge bg-primary">{{ $ticket->status }}</span></div><div class="panel p-4"><h5 class="fw-bold">Replies</h5>@foreach($ticket->replies as $reply)<div class="border rounded p-3 mb-2"><div class="small text-muted">{{ $reply->user?->name }} - {{ $reply->created_at->format('M d, Y H:i') }}</div>{{ $reply->message }}</div>@endforeach<form method="POST" action="{{ route('dashboard.tickets.reply',$ticket) }}" class="mt-3">@csrf<textarea name="message" class="form-control mb-2" required></textarea><button class="btn btn-primary">Reply</button></form></div>@endsection