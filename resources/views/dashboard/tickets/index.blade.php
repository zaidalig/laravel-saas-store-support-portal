@extends('layouts.dashboard') @section('page_title','My Tickets') @section('content')<div class="d-flex justify-content-end mb-3"><a href="{{ route('dashboard.tickets.create') }}" class="btn btn-primary">New Ticket</a></div><div class="panel p-3 table-responsive"><table class="table"><thead><tr><th>Ticket</th><th>Subject</th><th>Priority</th><th>Status</th><th></th></tr></thead><tbody>@forelse($tickets as $ticket)<tr><td>{{ $ticket->ticket_number }}</td><td>{{ $ticket->subject }}</td><td><span class="badge bg-warning">{{ $ticket->priority }}</span></td><td><span class="badge bg-primary">{{ $ticket->status }}</span></td><td><a href="{{ route('dashboard.tickets.show',$ticket) }}" class="btn btn-sm btn-outline-primary">View</a></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-4">No tickets found.</td></tr>@endforelse</tbody></table>{{ $tickets->links() }}</div>@endsection