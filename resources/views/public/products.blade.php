@extends('layouts.public') @section('title','Products') @section('content')<div class="container py-5"><div class="d-flex justify-content-between mb-4"><h1 class="fw-bold">Products & Services</h1><form><input name="search" class="form-control" placeholder="Search..." value="{{ request('search') }}"></form></div><div class="row g-4">@forelse($products as $product)<div class="col-md-4"><div class="panel p-4 h-100"><span class="badge badge-soft mb-2">{{ $product->category?->name }}</span><h5 class="fw-bold">{{ $product->name }}</h5><p class="text-muted">{{ $product->short_description }}</p><p class="fw-bold">${{ number_format($product->price,2) }} / {{ str_replace('_',' ',$product->billing_type) }}</p><a href="{{ route('products.show',$product->slug) }}" class="btn btn-primary">Details</a></div></div>@empty <div class="col-12"><div class="panel p-5 text-center text-muted">No active products found.</div></div>@endforelse</div><div class="mt-4">{{ $products->links() }}</div></div>@endsection