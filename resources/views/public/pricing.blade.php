@extends('layouts.public') @section('title','Pricing') @section('content')<div class="container py-5"><h1 class="fw-bold text-center mb-4">Pricing Plans</h1><div class="row g-4">@foreach($plans as $plan)<div class="col-md-4"><div class="panel p-4 h-100"><h4 class="fw-bold">{{ $plan->name }}</h4><h2 class="fw-bold">${{ number_format($plan->price,2) }}</h2><p class="text-muted">{{ $plan->duration_days }} days</p><ul>@foreach(explode("\n",$plan->features) as $feature)<li>{{ $feature }}</li>@endforeach</ul>@auth @if(auth()->user()->isCustomer())<form method="POST" action="{{ route('dashboard.subscriptions.store', $plan) }}">@csrf<button class="btn btn-primary w-100">Subscribe</button></form>@else<a href="{{ route('admin.dashboard') }}" class="btn btn-outline-primary w-100">Admin Dashboard</a>@endif @else<a href="{{ route('register') }}" class="btn btn-outline-primary w-100">Sign Up to Subscribe</a>@endauth</div></div>@endforeach</div></div>@endsection