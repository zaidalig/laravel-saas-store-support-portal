@extends('layouts.public') @section('title',$product->name) @section('content')<div class="container py-5"><div class="row g-4"><div class="col-lg-8"><div class="panel p-4"><span class="badge badge-soft">{{ $product->category?->name }}</span><h1 class="fw-bold mt-3">{{ $product->name }}</h1><p class="lead text-muted">{{ $product->short_description }}</p><div class="border-top pt-3">{!! nl2br(e($product->description)) !!}</div></div></div><div class="col-lg-4"><div class="panel p-4"><h3 class="fw-bold">${{ number_format($product->price,2) }}</h3><p class="text-muted">{{ ucfirst(str_replace('_',' ',$product->billing_type)) }} billing</p>@auth @if(auth()->user()->role === 'customer')<a href="{{ route('orders.place',$product) }}" class="btn btn-primary w-100">Place Order</a>@else <a href="{{ route('admin.products.index') }}" class="btn btn-primary w-100">Manage Product</a>@endif @else<a href="{{ route('login') }}" class="btn btn-primary w-100">Login to Order</a>@endauth</div></div></div></div>@endsection