@extends('layouts.public') @section('title','Track Order') @section('content')<div class="container py-5"><div class="panel p-4 mb-4"><h1 class="fw-bold">Track Order</h1><form method="POST" action="{{ route('track-order.result') }}" class="row g-2">@csrf <div class="col-md-5"><input name="order_number" class="form-control" placeholder="Order Number" required></div><div class="col-md-5"><input name="customer_email" type="email" class="form-control" placeholder="Email" required></div><div class="col-md-2"><button class="btn btn-primary w-100">Track</button></div></form></div>@isset($order)<div class="panel p-4">@if($order)<h4 class="fw-bold">{{ $order->order_number }}</h4><p>Status: <span class="badge bg-primary">{{ $order->status }}</span> Payment: <span class="badge bg-secondary">{{ $order->payment_status }}</span></p><p>Total: ${{ number_format($order->total,2) }}</p>@else<p class="text-muted mb-0">No order found for those details.</p>@endif</div>@endisset</div>@endsection